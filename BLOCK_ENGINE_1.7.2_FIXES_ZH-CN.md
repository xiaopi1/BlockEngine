# 方块引擎 1.7.2 修复说明

本次修改直接在方块引擎源码中完成，保留启动、账户、实例、资源下载、自动更新和 GPL-3.0 许可证功能。没有反编译任何 EXE，也没有把更新签名私钥放入源码。

## 已修复

1. 清理缓存时，前端传入后端暂不支持的缓存分类不会再导致整个清理操作失败；受支持分类仍会正常清理。
2. CurseForge 导入缺少受限制文件时，用户可选择已经手动下载的文件；文件名、大小、SHA-1 或 CurseForge 指纹验证通过后才会导入。
3. 下载统计按文件路径去重，并把已完成文件数与字节数限制在任务总量内，避免手动补文件后显示“已下载大于预期”。
4. Discord Rich Presence 连接、刷新与断开改为非阻塞并带超时；Discord 不可用时不会卡住 Minecraft 启动。
5. NSIS 自定义安装目录改用 UTF-16 环境变量传递，并安全处理状态文件参数，修复中文目录乱码及安装位置被忽略。
6. 首页不再等待 Minecraft 账户名称网络请求完成后才结束 Splash；账户服务缓慢时主窗口仍会按时显示。
7. 兼容 CurseForge 接口返回 `null` 的分类、版本、图库、哈希、游戏版本、依赖与模块集合。
8. 导入外部实例时，按 SHA-1 去重原生库解压任务，避免相同原生库并发写入导致 I/O 错误。

## 更新系统

- 客户端版本升级为 `1.7.2`。
- 官方更新接口固定为 `https://san2.top/blockengine/latest.php`。
- 自动更新开关可以正常保存：开启时检测并自动下载；关闭时只提醒，由用户点击下载。
- 更新包必须带同名 `.sig`，客户端在安装前会用内置公钥验证。
- `tools/update-test-shell` 提供独立的 1.7.1 更新测试壳，用于在正式发布前验证 1.7.2 更新。

## 发布前验证

- Rust 关键回归测试与安装器测试。
- Vue 类型检查和正式前端构建。
- 带 updater 功能的 Rust 检查。
- 签名 NSIS 安装包构建。
- 源码包私钥与缓存排除检查。

