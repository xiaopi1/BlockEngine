# 方块引擎 1.7.1 更新测试壳

这个独立小程序只验证正式更新链路：连接 `san2.top`、检测新版、下载、验证签名并启动安装。它不包含账户、实例、资源或启动功能，也不会读取正式启动器的数据。

先把签名后的 `1.7.2` 安装包和同名 `.sig` 上传到服务器，再上传两行 `version.txt`，最后运行 `build-windows.cmd` 生成 1.7.1 测试壳。

更新签名私钥只能存放在 `E:\codex\private\BlockEngine-Update-Keys`，禁止复制进本目录或 Git 仓库。

